## APPLY BASIC NTP CONFIG **
#if($ntp.authenticate == true)
ntp authenticate
#else
no ntp authenticate
#end
#if($ntp.logging == true)
ntp logging
#else
no ntp logging
#end
## APPLY NTP AUTH KEY CONFIG **
#foreach($k in $ntp.auth_keys)
ntp authentication-key $k.id md5 $k.text
#end
## APPLY NTP SERVER CONFIG **
#foreach($s in $ntp.servers)
ntp server $s.ip key $s.key_id
#end
